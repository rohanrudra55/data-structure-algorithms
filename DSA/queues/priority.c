#include<stdio.h>#include<stdlib.h>typedef struct node{	int data;	int priority;	struct node *next;}node;node *create(int data,int priority);node *insert( node *);node *delete( node *);void display( node *);int main(){     	int option;     	node *start=NULL;      	printf("\n *****MAIN MENU*****");     	printf("\n 1. INSERT");     	printf("\n 2. DELETE"); 		printf("\n 3. DISPLAY");     	printf("\n 4. EXIT");     	printf("\n Enter your option : ");     	do{         scanf( "%d", &option);         switch(option){                  case 1:                        start=insert(start);                        break;                  case 2:                        start = delete(start);                        break;                  case 3:                  		display(start);                  		break;                  	}                  }while(option!=4);              }node *create(int data,int priority){	node *new_node=(node*)malloc(sizeof(node));	new_node->data=data;	new_node->priority=priority;	new_node->next=NULL;	return new_node;}node *insert(node *start){	int val,pri;	printf("Enter the value it's priority: ");	scanf("%d %d ",&val,&pri);	node *temp=create(val,pri);	if(start == NULL || pri<start->priority){		temp->next=start;		start=temp;	}	else{		node *it=start;		while(it->next != NULL && it->next->priority <= pri)			it=it->next;		temp->next = it->next;     	it->next = temp;		}	return start;}node *delete(node* start){	node *ptr;	if(start == NULL) printf("\n UNDERFLOW" );		else{	ptr = start;	printf("\n Deleted item is: %d", ptr->data);	start = start->next;	free(ptr); 	} 	return start; } void display(node * start){ 	node *ptr=start; 	if(start==NULL) printf("Empty! \n"); 	else{	 	while(ptr!=NULL){	 		printf("Data: %d Priority: %d \n",ptr->data,ptr->priority);	 		ptr=ptr->next;	 	}	 } }